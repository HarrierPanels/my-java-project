pipeline {
    agent {
        label 'local1'
    }
    environment {
        AWS_CREDS = credentials('aws-creds')
    }

    stages {
        stage('Example AWS') {
            steps {
                sh 'aws --version"'
                sh 'aws ec2 describe-instances'
            }
        }
    }
}
